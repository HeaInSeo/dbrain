# DBrain repository agent entrypoint

This repository is in PKG-M0 contract/schema freeze.

Before modifying anything:

1. Read `docs/PKG-M0.md`.
2. Treat `docs/contracts/` as WORKING / NON-NORMATIVE until explicitly frozen.
3. Do not invent missing authority or implementation semantics.
4. Unknown or ambiguous required authority => `UNRESOLVED`.
5. Agent-local notes are not canonical knowledge.
6. Do not implement SQLite/FTS, embeddings, graph DB, distributed services, or full CLI behavior during PKG-M0 unless explicitly authorized.

Current project name: **DBrain — Development Brain**.
